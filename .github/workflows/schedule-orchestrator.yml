name: Daily Orchestrator + Monitoring Run

on:
  schedule:
    # Her gün sabah 09:00 UTC'de çalıştır
    - cron: '0 9 * * *'
  workflow_dispatch: # Manuel tetikleme için açık

jobs:
  run-orchestrator-and-monitoring:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Orchestrator
        run: |
          PYTHONPATH=src python src/orchestrator.py

      - name: Run Monitoring & Health Check
        run: |
          PYTHONPATH=src python -m src.monitor_and_alert

      - name: Upload logs and monitoring results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: daily-orchestrator-monitoring-logs
          path: |
            artifacts/logs/
            artifacts/alerts.log
            artifacts/Usage_Log.md
            artifacts/state.json